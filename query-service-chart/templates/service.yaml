apiVersion: v1
kind: Service
metadata:
  name: {{ include "query-service-chart.fullname" . }}
  labels:
    {{- include "query-service-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "query-service-chart.selectorLabels" . | nindent 4 }}
