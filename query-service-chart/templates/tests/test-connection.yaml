apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "query-service-chart.fullname" . }}-test-connection"
  labels:
    {{- include "query-service-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "query-service-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
